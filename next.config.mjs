/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
          'm.media-amazon.com',
          'rukminim2.flixcart.com',
          'store.arduino.cc',
          'little-flower.vercel.app',
        ]
      },
};

export default nextConfig;
